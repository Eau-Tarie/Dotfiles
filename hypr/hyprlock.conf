# sample hyprlock.conf
# for more configuration options, refer https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock
#
# rendered text in all widgets supports pango markup (e.g. <b> or <i> tags)
# ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#general-remarks
#
# shortcuts to clear password buffer: ESC, Ctrl+U, Ctrl+Backspace
#
# you can get started by copying this config to ~/.config/hypr/hyprlock.conf
#

#===FONT===
$primary_font = Firacode
$clock_font = ds-digital
#========


#===General Hyprlock behavior===
general {
    hide_cursor = false
    grace = 10 #  set it to 2
    ignore_empty_input = true
}
#===============================

# uncomment to enable fingerprint authentication
# auth {
#     fingerprint {
#         enabled = true
#         ready_message = Scan fingerprint to unlock
#         present_message = Scanning...
#         retry_delay = 250 # in milliseconds
#     }
# }

#===Animations===
animations {
    enabled = true
    bezier = linear, 1, 1, 0, 0
    animation = fadeIn, 1, 5, linear
    animation = fadeOut, 1, 5, linear
    animation = inputFieldDots, 1, 2, linear
}
#================

#===Background-Lockscreen===
background {
    monitor =
    path = screenshot 
    blur_passes = 3
    blur_size: 0
}

#=====Password-Field====
input-field {
    monitor =
    size = 20%, 5%
    outline_thickness = 3
    inner_color = rgba(255, 255, 255, 1.0) # fill in white

    outer_color = rgba(33ccffee) rgba(00ff99ee) 45deg
    check_color = rgba(00ff99ee) rgba(ff6633ee) 120deg
    fail_color = rgba(ff6633ee) rgba(ff0066ee) 40deg
    
    font_color = rgb(143, 143, 143)
    fade_on_empty = false
    fade_on_check = true
    rounding = 15

    font_family = $font
    placeholder_text =   Password
    fail_text = $FAIL !
    #fail_text = $PAMFAIL

    # uncomment to use a letter instead of a dot to indicate the typed password
    # dots_text_format = *
    # dots_size = 0.4
    dots_spacing = 0.3
    
    # uncomment to use an input indicator that does not show the password length (similar to swaylock's input indicator)
    # hide_input = true

    position = 0, -70
    halign = center
    valign = center
}

# TIME (TODO: to delete)
label {
    monitor =
    #text = $TIME # ref. https://wiki.hyprland.org/Hypr-Ecosystem/hyprlock/#variable-substitution
    text = cmd[update:60000] date +"%H<span size='xx-small'>H</span><br/>%M"
    text_align = center
    font_size = 70
    font_family = $digital_font

    position = 0, 160
    halign = top
    valign = center
}


#==Clock image (TODO: Turn it to image)==
label {
   monitor =
   text = 
   font_size = 40
   position = 0, 280
   valign = center
   halign = center
}
#====================

#== Work in progress placeholder==
label {
   monitor =
   text =  Work in progress
   font_size = 70
   color = rgba(255, 0, 0, 1.0)
   valign = top
   halign = center
}

#====Hour text====
label {
   monitor =
   text = cmd[1000] date +"%H<span size='xx-small'>H</span>"
   font_size = 70
   font_family = $digital_font
   color = rgba(250, 250, 100, 1.0)
   position = 0, 200
   valign = center
   halign = center
}

#==Minutes text==
label {
   monitor =
   text = cmd[update:1000] date +"%M"
   font_size = 70
   font_family = $digital_font
   color = rgba(100, 100, 100, 1.0)
   position = 0, 120
   valign = center
   halign = center
}

# DATE (TODO: keep calendar, remove hour, position on it on better place)
label {
    monitor =
    text = cmd[update:60000] date +"%A, %d %B %Y - %H:%M:%S" # update every 60 seconds
    font_size = 25
    font_family = $font
    position = -30, -150
    halign = right
    valign = top
}

#==Second text==
label {
    monitor =
    text = cmd[update:1000] date +"%S"
    font_size = 22
    color = rgba(0, 0, 0, 1.0)
    font_family = $font
    position = 100,70
    halign = center
    valign = center
}

#==Unlock button (TODO: place shape arround to make a better button==
label {
    monitor = 
    text =   Unlock  
    font_size = 20
    onclick = hyprctl switchxkblayout all next
    position = 260, -70
    halign = center
    valign = center
}

#==Attemps text (TODO: draw a red shape ?
label {
   monitor =
   text = Remaining attempts before lock: <b><span foreground="orange" size="x-small" >$ATTEMPTS / 5</span></b>
   font_size = 20
   position = 0, -140
   valign = center
   halign = center
}

#==Welcome message (TODO: remove it ? Keep the user on the bottom right corner of the screen)
label {
   monitor =
   text = 🌊 Seal you later <span foreground="blue" text-transform="capitalize">🦭 $USER</span>
   font_size = 20
   valign = center
   halign = center
}

# clock shape
shape {
    monitor =
    size = 250, 250
    color = rgba(0, 0, 0, 0.0) # no fill
    rounding = -1 # circle
    border_size = 4
    border_color = rgba(0, 207, 230, 1.0)

    position = 0, 160
    halign = center
    valign = center
}

# second shape
shape {
   monitor =
   size = 75, 75
   color = rgba(0, 250, 250, 1.0)
   rounding = -1
   border_size = 2
   border_color = rgba(207, 0, 230, 0.0)
   position = 100, 70
   halign = center
   valign = center
}

# bottom zone
shape {
   monitor =
   size= 100%, 0
   color = rgba(250, 250, 250, 1.0)
   border_size=2
   border_color = rgba(207, 0, 230, 1.0)
   halign = center
   valign = bottom
   position = 0, 60
}

# add button [suspend, hibernate, reboot, shutdown]
